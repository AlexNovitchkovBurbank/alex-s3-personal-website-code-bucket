{
  "name": "alex-personal-website-code-bucket",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dp:np:w": "sls deploy --verbose -r us-west-2 -s np --force",
    "dp:rm:w": "sls remove --verbose -r us-west-2 -s np",
    "s3 deploy": "aws s3 sync dist s3://alex-page-np/ --delete"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
